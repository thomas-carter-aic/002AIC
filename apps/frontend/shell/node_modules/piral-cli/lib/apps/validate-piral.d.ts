import { LogLevels } from '../types';
export interface ValidatPiralOptions {
    /**
     * Sets the root directory containing the Piral instance's project.json.
     */
    entry?: string;
    /**
     * Sets the log level to use (1-5).
     */
    logLevel?: LogLevels;
}
export declare const validatePiralDefaults: ValidatPiralOptions;
export declare function validatePiral(baseDir?: string, options?: ValidatPiralOptions): Promise<void>;
