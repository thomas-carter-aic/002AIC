import { Agent } from 'https';
import { PublishScheme } from '../types';
export declare function publishPackageEmulator(directory: string, file: string, url?: string, interactive?: boolean, apiKey?: string): Promise<void>;
export declare function publishWebsiteEmulator(version: string, url: string, apiKey: string, mode: PublishScheme, directory: string, files: Array<string>, interactive: boolean, headers?: Record<string, string>, agent?: Agent): Promise<import("./http").PostFormResult>;
