import { Agent } from 'https';
export declare function updateFromEmulatorWebsite(targetDir: string, manifestUrl: string, httpsAgent: Agent, interactive: boolean): Promise<void>;
export declare function scaffoldFromEmulatorWebsite(rootDir: string, manifestUrl: string, httpsAgent: Agent): Promise<{
    name: string;
    path: string;
}>;
