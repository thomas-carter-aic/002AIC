import type { Bundler, BaseBundleParameters } from '../types';
export declare function callDynamic<T extends BaseBundleParameters>(name: string, path: string, args: T, exec?: string): Promise<Bundler>;
export declare function callStatic<T extends BaseBundleParameters>(name: string, path: string, args: T, exec?: string): Promise<Bundler>;
